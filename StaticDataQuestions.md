
# For large static data tables -- is reloading preferred?

How often does the data change?

How much of the data changes? 

Do the static data tables have foreign keys? Indexes?

If reloading makes sense, is there an obvious option for checking if a reload is needed?